export interface DynamicPropInterface {
    name: string,
    propId: number,
    privateComment: string,
    propPos: number,
    propIsRequire: boolean,
    propIsCanAsFilter: boolean,
    propComment: string,
}
